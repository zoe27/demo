---
name: drone
kind: pipeline
type: docker

volumes:
  - name: targetJar
    host:
      path: /home/repository/l

steps:
  - name: build
    image: redis:6.2.1
    commands:
    - echo "hello world"
    - echo "this is the first time to test drone"

  - name: test
    image: maven:3-jdk-8
    volumes:
      - name: targetJar
        path: /home/repository
    commands:
      - mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
      - mvn test -B
      - pwd
      - ls
      - ls target
      - ls targetJar
      - cp -rf target/* targetJar

  - name: deploy
    image: redis:6.2.1
    commands:
      - echo "start to deploy service"

trigger:
  branch:
    - master
  event:
    - push

